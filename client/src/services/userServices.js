const register = (userData) => {}; // validate if passes match and other validation => send to backend for DB validation

const login = (userData) => {}; //chechk if passwords match send to backEnd for further validation with DB entity

const userServices = {
    register,
    login,
};

export default userServices;
